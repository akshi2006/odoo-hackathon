# Optional: set when the frontend is served separately from the API (production).
# Leave empty for local dev — Vite proxies /api to the backend (see vite.config.js).
# VITE_API_URL=http://localhost:4000
